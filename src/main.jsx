import React from 'react'
import ReactDOM from 'react-dom/client'
import EORModelApp from './EOR_Model.jsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EORModelApp />
  </React.StrictMode>,
)
